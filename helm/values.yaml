# Default values for wc2026 chart
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: "${DOCKER_USERNAME}/wc2026"
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

replicaCount: 1

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
